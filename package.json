{
  "name": "kozo",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn nx run core:build",
    "watch": "(yarn nx run webview:dev&) && yarn nx run core:watch"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "15.3.0",
    "prettier": "^2.6.2",
    "typescript": "^4.8.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
